import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Project } from "@/lib/content";
export function ProjectCard({ project }: { project: Project }) { return <Link href={`/projects/${project.slug}`} className="group card block overflow-hidden p-6 sm:p-7"><div className="mb-16 flex items-start justify-between"><span className="font-mono text-xs text-zinc-500">{project.year}</span><ArrowUpRight size={18} className="text-zinc-500 transition duration-300 group-hover:-translate-y-1 group-hover:translate-x-1 group-hover:text-violet-400 group-hover:drop-shadow-glow"/></div><h3 className="text-xl font-bold tracking-tight">{project.title}</h3><p className="mt-3 max-w-sm text-sm leading-6 text-zinc-400">{project.summary}</p><div className="mt-6 flex flex-wrap gap-2">{project.status && <span className="rounded-full bg-amber-500/10 px-2.5 py-1 font-mono text-[10px] text-amber-300">{project.status}</span>}{project.tags.map(tag => <span key={tag} className="rounded-full border border-white/[.08] px-2.5 py-1 font-mono text-[10px] text-zinc-400">{tag}</span>)}</div></Link> }
